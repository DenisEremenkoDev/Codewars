function animal(obj) {
    return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}




// напиши функцию animal, принимающую 1 параметр: objнапример, так:

// {name:"dog",legs:4,color:"white"}
// и вернуть строку вроде этой:

// "This white dog has 4 legs."